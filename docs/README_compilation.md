# Documentation de compilation

Ce répertoire contient la documentation relative à la compilation de l'application AnnotationPhotosGPT.

## Fichiers présents

- **Compilation_PyInstaller_Procedure.pdf** :
  Un guide PDF complet pour compiler un script `.py` en exécutable `.exe` avec PyInstaller.

## Utilisation recommandée

1. Lire le PDF pour comprendre la procédure.
2. Utiliser les fichiers `.bat` fournis dans le dossier `scripts/` :
   - `compile_convert_transcription_docx.bat` : pour recompiler
   - `run_convert_transcription_docx.bat` : pour exécuter l'application

## Remarques

- Le `.exe` généré est autonome (pas besoin de Python installé sur une autre machine).
- Conservez ce document dans le projet pour maintenance ou transfert ultérieur.

---
Mis à jour automatiquement avec l'assistance de GPT le 24 avril 2025.